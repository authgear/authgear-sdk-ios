---
ignore:
  # CVE-2026-54620 / GHSA-j7fr-3v8c-3qc3: use-after-free in SQLite aggregate
  # function callbacks, fixed in sqlite3 gem >= 2.9.5.
  #
  # CVE-2026-54619 / GHSA-28hh-pr2h-2w89: use-after-free when redefining
  # SQLite functions with different arity, also fixed in sqlite3 gem >= 2.9.5.
  #
  # sqlite3 is a transitive dependency pulled in by jazzy (~> 1.3), which is
  # used only locally to generate API documentation and is not part of the
  # shipped SDK. jazzy has not relaxed its sqlite3 constraint in any released
  # version (checked up to 0.15.4, latest as of 2026-08-04), so bundler cannot
  # resolve a patched sqlite3 while jazzy stays in the Gemfile. Remove these
  # ignores once jazzy allows sqlite3 >= 2.9.5 or is replaced.
  - CVE-2026-54620
  - CVE-2026-54619
